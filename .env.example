# ===========================================
# NESTWELL - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control!

# -------------------------------------------
# CLERK - Authentication & Billing
# -------------------------------------------
# Get these from: https://dashboard.clerk.com
# 1. Create a new application
# 2. Enable Billing in settings
# 3. Create "Free" and "Agent" plans

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
CLERK_SECRET_KEY=sk_test_xxxxx

# Clerk redirect URLs (customize if needed)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# Clerk Webhook Secret (if using webhooks)
CLERK_WEBHOOK_SECRET=whsec_xxxxx

# -------------------------------------------
# SANITY - Content Management
# -------------------------------------------
# Get these from: https://sanity.io/manage
# 1. Create a new project
# 2. Go to API settings for tokens

NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production

# Write token for server-side mutations (keep this secret!)
# Create at: sanity.io/manage > API > Tokens > Add API Token
SANITY_API_TOKEN=your-write-token

# -------------------------------------------
# MAPBOX - Interactive Maps & Geocoding
# -------------------------------------------
# Get this from: https://account.mapbox.com/access-tokens/
# Default public token works for development

# Client-side token (for map display)
NEXT_PUBLIC_MAPBOX_TOKEN=pk.xxxxx

# Server-side token (for geocoding API)
MAPBOX_ACCESS_TOKEN=pk.xxxxx

# -------------------------------------------
# APP CONFIGURATION
# -------------------------------------------
# Used for metadata, og:image URLs, etc.

NEXT_PUBLIC_APP_URL=http://localhost:3000

# -------------------------------------------
# NOTES
# -------------------------------------------
# 
# NEXT_PUBLIC_ prefix = exposed to browser (safe for public keys)
# No prefix = server-only (keep these secret!)
#
# For production on Vercel:
# 1. Go to Project Settings > Environment Variables
# 2. Add all variables above
# 3. Redeploy after changes
